import { RecipeCookClient } from './RecipeCookClient';

export const dynamic = 'force-dynamic';

export const metadata = {
  title: 'POC Cuisine guidée · Editor',
};

export default function RecipeCookPocPage() {
  return <RecipeCookClient />;
}
